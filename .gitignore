# Wrangler config (contains sensitive database IDs)
wrangler.toml

# Generated by wrangler types (contains sensitive database IDs and keys)
worker-configuration.d.ts

# Wrangler local state and credentials cache
.wrangler/
wrangler-account.json
wrangler-account.json*

# Local development env/secrets
.env
.env.*
.dev.vars
.dev.vars.*

# Local databases and temporary artifacts
*.db
*.db-*
*.sqlite
*.sqlite3

# Build output and dependencies (if added later)
dist/
node_modules/
